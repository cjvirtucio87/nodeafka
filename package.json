{
  "name": "kafka-quick-start",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "tsc && node build/main",
    "serve": "tsc && node build/server",
    "describe": "tsc && node build/command --command=describe",
    "create": "tsc && node build/command --command=create",    
    "zookeeper": "tsc && node build/server --type=zookeeper",
    "producer": "tsc && node build/producer",
    "consumer": "tsc && node build/consumer",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.4"
  }
}
